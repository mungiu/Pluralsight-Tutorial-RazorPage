@page "/menu/{slug}"
@model ItemModel
@{
    ViewData["Title"] = "About";
}
<!--"{id}" tells razor that any link to this page should include and
    Id path which will work with our rout ID helper
    "{slug} a friendly URL path for a content item: EG: "pumpkin-bread"
    :alpha} forces that the slug place holder only includes alphabet characters
    :double} must be int parsable
    :minlength(n)}
    :datetime} must be formated as date time
    :guid} must be formated as guid
    EG: "{slug:alpha:minlength(5)}""
    NOTE: it is possible to create custom route constraints using "IRouteConstraint"-->

<div class="container">
    <h1 class="mt-4 mb-3">
        @Model.Item.Name
    </h1>
    <hr />
    <h3>A Taste of Autumn.</h3>

    <div class="row">
        <div class="col-lg-8">
            <img class="img-fluid rounded" src="~/images/Menu/@Model.Item.ImageFile" alt="">
            <hr>
            <p>Available starting at 8:00 AM</p>
            <hr>
            <p class="lead">@Model.Item.Summary</p>
            <blockquote class="blockquote">
                <p class="mb-0">I feel at home every time I take a bite.</p>
                <footer class="blockquote-footer">
                    Actual customer
                    <cite title="Source Title">Happy Customer</cite>
                </footer>
            </blockquote>
            <p>@Model.Item.Description</p>
            <hr>
        </div>
        <div class="col-md-4">
            <div class="card">
                <h5 class="card-header">Nutrition Information</h5>
                <div class="card-body">
                    <p>
                        Note: Nutrition information is an estimate only.
                        We back our goods fresh every day, so nutrition values
                        may vary depending on preparation and serving size.
                    </p>
                    <ul>
                        <li>Calories: @Model.Item.Calories</li>
                        <li>Total Fat: @Model.Item.Fat%</li>
                        <li>Saturated Fat: @Model.Item.SaturatedFat%</li>
                        <li>Fiber: @Model.Item.Fiber%</li>
                        <li>Sugar: @Model.Item.Sugar</li>
                    </ul>
                </div>
            </div>
            <hr />

            @await Component.InvokeAsync("PopularBrews", new { count = 3 });
        </div>
    </div>
</div>